<%= form_for(@agenda_reservation) do |f| %>
  <% if @agenda_reservation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agenda_reservation.errors.count, "error") %> prohibited this agenda_reservation from being saved:</h2>

      <ul>
      <% @agenda_reservation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.text_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :resource_id %><br />
    <%= f.text_field :resource_id %>
  </div>
  <div class="field">
    <%= f.label :start_date_time %><br />
    <%= f.datetime_select :start_date_time %>
  </div>
  <div class="field">
    <%= f.label :end_date_time %><br />
    <%= f.datetime_select :end_date_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
